<div class="mt-12 text-center">
    <a href="{{ $href ?? '#' }}"
        class="inline-block bg-gray-600 hover:bg-yellow-400 hover:text-gray-600 text-white px-6 py-3 rounded-lg font-medium transition-all">
        {{ $slot }}
    </a>
</div>
